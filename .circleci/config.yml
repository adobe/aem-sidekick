version: 2.1

jobs:
  noop:
    docker:
      - image: cimg/base:stable
    steps:
      - run:
          name: No-op (migrated to GitHub Actions)
          command: echo "This project has been migrated to GitHub Actions. See .github/workflows/ for the new CI/CD pipeline."

workflows:
  version: 2
  noop:
    jobs:
      - noop
